# ──────────────────────────────────────────────────────────────────
# wyckoff-pro 环境变量示例
# 复制此文件为 .env.local 并填入真实值（.env.local 已在 .gitignore 中）
# ──────────────────────────────────────────────────────────────────

# 应用标题（可选，默认 AI威科夫Pro）
VITE_APP_TITLE=AI威科夫Pro

# ── 未来后端代理配置（当前为纯前端，以下为上线后参考） ──────────────
# 后端 API 地址（替换 vite proxy 时填写）
# VITE_API_BASE_URL=https://your-backend.com

# LLM 代理端点（推荐：将 API Key 移到后端，前端调此地址）
# VITE_LLM_PROXY_URL=https://your-backend.com/api/llm-proxy

# ── 第三方服务（可选） ────────────────────────────────────────────
# Sentry DSN（错误监控）
# VITE_SENTRY_DSN=https://xxx@sentry.io/xxx
